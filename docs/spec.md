# 大規模ブロック崩しゲーム「Mega-Block Breaker」仕様・設計書

## 1. ゲームコンセプト

15,000個という圧倒的な数のブロックを破壊する快感と、26種類のアルファベット・アイテムによる予測不能なパワーアップを楽しむ、高難易度・高爽快感のブロック崩し。

## 2. 基本スペック

- **キャンバス**: 500 × 800 ピクセル（CSS px）
- **ブロック**: 3x3ピクセル、150列 × 100行（計15,000個）→ ブロック領域 450 × 300px
- **ボール**: 基本3x3ピクセル
- **パドル**: 80x10ピクセル
- **ライフ制**: 初期ライフ3。スコア（例：50,000点ごと）でエクステンド。

### レイアウトゾーン（y座標）

| ゾーン | y範囲 | 高さ |
|--------|-------|------|
| HUD（スコア・ライフ・タイマー） | 0 – 40px | 40px |
| ブロックグリッド | 40 – 340px | 300px |
| プレイエリア（ボール・アイテム落下） | 340 – 750px | 410px |
| パドルゾーン | 750 – 800px | 50px |

ブロックグリッドは水平方向に中央揃え（左右 25px マージン）。
    

## 3. 提案：未定義仕様の定義

### 3.1 クリア条件

- **全消しまたは特定条件**: 画面内のブロックを90%以上破壊、あるいは特定の「コアブロック」を破壊することでステージクリア。
    
- **時間制限**: 膨大なブロック数があるため、制限時間は長めに設定（またはアイテムで延長可能にする）。
    

### 3.2 物理・挙動

- **貫通属性**: 通常状態では跳ね返るが、アイテムにより「貫通」状態になるとブロックを破壊しながら直進。
    
- **速度加速**: 破壊数に応じて徐々にボールスピードが上昇。
    

## 4. 提案：面白さを高める追加仕様

### 4.1 コンボシステム

- ボールがパドルに触れずに連続してブロックを壊すとスコア倍率が上昇。
    
- 15,000個という特性上、連鎖的な爆発（ボム）などが起きるようにすると爽快感が増します。
    

### 4.2 ビジュアル効果

- **パーティクル**: ブロック破壊時に火花が散る演出。
    
- **スクリーンシェイク**: 連続破壊時に画面が微細に揺れる。
    

## 5. アルファベット・アイテム効果（A-Z 26種）

アイテムは落下するアルファベットとして出現します。

|文字|効果名|内容|
|---|---|---|
|**A**|Aim|ボールの軌道予測線を表示|
|**B**|Bomb|衝突時に周囲のブロックを巻き込んで爆発|
|**C**|Catch|パドルでボールをキャッチして任意のタイミングで発射|
|**D**|Double|ボールの数が2倍に増える|
|**E**|Expand|パドルの横幅が広がる|
|**F**|Fire|ボールが火の玉になり、耐久力のあるブロックを一撃で破壊|
|**G**|Gravity|ブロックがゆっくりと下に降りてくる（ピンチ！）|
|**H**|Heal|ライフ（残機）が1増える|
|**I**|Invisible|ボールが点滅して見えにくくなる（デバフ）|
|**J**|Jump|パドルが上下に移動可能になる|
|**K**|Killer|ボールが鋭利になり、ブロックを貫通する|
|**L**|Laser|パドルから弾を発射してブロックを直接壊せる|
|**M**|Magnet|パドルの中心にボールが吸い寄せられる|
|**N**|Normal|全ての特殊状態をリセットして通常に戻す|
|**O**|Obstacle|画面中央に障害物が出現（デバフ）|
|**P**|Power|ボールのサイズが大きくなり、攻撃判定拡大|
|**Q**|Quake|画面が揺れてランダムにブロックが壊れる|
|**R**|Reverse|パドルの左右操作が反転する（デバフ）|
|**S**|Slow|ボールの移動速度が一時的に低下|
|**T**|Twin|パドルが2つに分裂する|
|**U**|UFO|画面上部からUFOが出現し、ブロックをさらっていく|
|**V**|Vortex|ボールがブラックホール化し、周囲のブロックを吸い込む|
|**W**|Warp|ボールが画面端でループするようになる|
|**X**|X-ray|特定の隠しアイテム（ボーナス）が透けて見える|
|**Y**|Yield|スコア倍率が一時的に2倍になる|
|**Z**|Zap|画面内のランダムな列に電撃が走り、一列消去|

## 6. 技術設計のポイント

- **描画最適化**: 15,000個のオブジェクトを個別のDOMで扱うと重いため、HTML5 Canvas APIを使用し、一括描画。
    
- **衝突判定**: グリッドベースの衝突判定（ボールの座標から配列のインデックスを計算）を行い、15,000回ループを回さない設計にする。
    
- **パラメータ管理**: すべての定数を単一の `CONFIG` オブジェクトで管理する。

```js
const CONFIG = {
  CANVAS_W: 500, CANVAS_H: 800,
  HUD_H: 40,
  BLOCK_COLS: 150, BLOCK_ROWS: 100, BLOCK_W: 3, BLOCK_H: 3,
  BLOCK_OFFSET_X: 25,   // (500 - 150*3) / 2
  BLOCK_OFFSET_Y: 40,   // HUD_H
  BALL_SIZE: 3,
  PADDLE_W: 80, PADDLE_H: 10, PADDLE_Y: 750,
};
```

- **HiDPI（Retina）対応**: `devicePixelRatio` に応じて canvas の物理ピクセルをスケールし、CSS サイズは固定することで 3px ブロックがぼやけないようにする。

```js
const dpr = window.devicePixelRatio || 1;
canvas.width  = CONFIG.CANVAS_W * dpr;
canvas.height = CONFIG.CANVAS_H * dpr;
canvas.style.width  = CONFIG.CANVAS_W + 'px';
canvas.style.height = CONFIG.CANVAS_H + 'px';
ctx.scale(dpr, dpr);
```